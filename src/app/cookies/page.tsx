import { LegalDoc } from '@/components/legal-doc';

export default function CookiesPage() {
  return (
    <LegalDoc title="Cookie notice">
      <p>
        Essential cookies keep the session on this device. We do not run advertising cookies. Analytics, if enabled
        later, will be listed here before they fire.
      </p>
    </LegalDoc>
  );
}
