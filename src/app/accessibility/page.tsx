import { LegalDoc } from '@/components/legal-doc';

export default function AccessibilityPage() {
  return (
    <LegalDoc title="Accessibility">
      <p>
        OptiTalent aims for WCAG 2.1 AA on the screens we ship: visible focus, keyboard access on forms and the
        command palette, and contrast on forest-ink text against paper. Screen-reader QA on every flow is not
        complete; report gaps to support@optitalent.com.
      </p>
    </LegalDoc>
  );
}
